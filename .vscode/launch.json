{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                // This is the corrected path. It maps the exact path your server sees
                // to the workspace folder you have open in VS Code.
                "C:\\Users\\anwar\\Local Sites\\cannarewards-api\\app\\public\\wp-content\\plugins\\cannarewards-engine": "${workspaceFolder}"
            }
        }
    ]
}